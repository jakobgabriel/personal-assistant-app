{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Was die Oberflaeche darf. Bewusst knapp: alle Netzzugriffe passieren in Rust, deshalb braucht das Webview weder HTTP noch Dateisystem.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "deep-link:default",
    "log:default"
  ]
}
